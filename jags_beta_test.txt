model{
m0~ dnorm(0, .001)
m1~ dnorm(0, .001)
m2~ dnorm(0, .001)
m3~ dnorm(0, .001)
m4~ dnorm(0, .001)
m5~ dnorm(0, .001)
m6~ dnorm(0, .001)
m7~ dnorm(0, .001)
m8~ dnorm(0, .001)
tau ~ dgamma(.1,.1)
for(i in 1:N){
y[i] ~ dbeta(p[i], q[i])
p[i] <- mu[i] * tau
q[i] <- (1 - mu[i]) * tau
logit(mu[i]) <- m1 * x1[i] + m2 * x2[i] + m3 * x3[i] + m4 * x4[i] + m5 * x5[i] + m6 * x6[i] + m7 * x7[i] + m8 * x8[i] +  m0
}
}
